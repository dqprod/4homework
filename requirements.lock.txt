# 实际安装版本锁定（Phase 2 — add Supabase SDK）
# 生成时间：2026-06-14
# 用途：pip install -r requirements.lock.txt 一键复现
#
# Python 3.9 兼容性提示：
# - eval_type_backport 解决 pydantic 2.x + Python 3.9 的 `str | None` 语法问题
# - greenlet 3.0+ 是 SQLAlchemy 2.0 async 必需
# - pytest-asyncio 0.21.x 兼容 pytest 7.4
# - supabase 2.5.3 及其依赖链经 Python 3.9 验证通过

# --- 直接依赖（设计要求）---
fastapi==0.115.5
uvicorn==0.32.0
SQLAlchemy==2.0.25
aiosqlite==0.19.0
pydantic==2.9.2
python-multipart==0.0.6
pytest==7.4.4
pytest-asyncio==0.21.1
httpx==0.27.2           # 被 supabase 升级到此版本

# --- Supabase SDK（Phase 2 新增）---
# 主包
supabase==2.5.3
# supabase-py 子模块（自动拉但显式锁住防回归）
gotrue==2.12.4          # Auth
postgrest==0.16.11      # REST 查询
storage3==0.7.7         # Storage
realtime==1.0.6         # Realtime 订阅
supafunc==0.4.7         # Edge Functions
PyJWT==2.13.0           # JWT 解析
StrEnum==0.4.15         # supabase 枚举用
deprecation==2.1.0      # supabase 内部用
# HTTP/2 传输链（supabase 通过 h2 库走 http2）
h2==4.3.0
hpack==4.1.0
hyperframe==6.1.0
websockets==12.0        # Realtime 订阅用

# --- 间接依赖（被上面拉进来的，固定版本防回归）---
greenlet==3.0.3
anyio==4.6.2.post1
pydantic_core==2.23.4
tenacity==9.0.0

# --- 3.9 兼容补丁（eval_type_backport 解决 `str | None` 语法）---
# 注意：pip 包名是 eval_type_backport，import 名是 eval_type_backport
eval-type-backport==0.2.0